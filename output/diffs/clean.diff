diff --git a/notifications/email.py b/notifications/email.py
index 0000000..a1b2c3d 100644
--- /dev/null
+++ b/notifications/email.py
@@ -0,0 +1,38 @@
+"""Email notification helpers."""
+
+from __future__ import annotations
+
+import smtplib
+from email.mime.text import MIMEText
+from typing import Sequence
+
+from config import settings
+
+
+def send_email(
+    recipients: Sequence[str],
+    subject: str,
+    body: str,
+) -> None:
+    """Send a plain-text email to one or more recipients.
+
+    Raises:
+        smtplib.SMTPException: if the SMTP server rejects the message.
+    """
+    if not recipients:
+        raise ValueError("recipients must not be empty")
+
+    message = MIMEText(body, "plain")
+    message["Subject"] = subject
+    message["From"] = settings.EMAIL_FROM
+    message["To"] = ", ".join(recipients)
+
+    with smtplib.SMTP(settings.SMTP_HOST, settings.SMTP_PORT) as server:
+        if settings.SMTP_USE_TLS:
+            server.starttls()
+        if settings.SMTP_USERNAME:
+            server.login(settings.SMTP_USERNAME, settings.SMTP_PASSWORD)
+        server.sendmail(settings.EMAIL_FROM, list(recipients), message.as_string())
diff --git a/notifications/__init__.py b/notifications/__init__.py
index 0000000..e4a1b2c 100644
--- /dev/null
+++ b/notifications/__init__.py
@@ -0,0 +1,3 @@
+"""Notification subsystem."""
+
+from .email import send_email
